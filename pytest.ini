[pytest]
testpaths = tests
addopts = -q -ra
markers =
    slow: tests that load real models / download weights (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
